export enum ROUTES {
    DIALOGS = "/dialogs",
    PROFILE = "/profile/:userId",
    NEWS = "/news",
    MUSIC = "/music",
    SETTINGS = "/settings",
    USERS = "/developers",
    LOGIN = "/login",
    PAGE_NOT_FOUND = "/404"
}