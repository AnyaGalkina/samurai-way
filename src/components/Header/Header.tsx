import React from "react";
import styles from "./Header.module.css";

// style = {header: 'Header_header__Lhnr8'}

const Header: React.FC = () => {
    return (
            <header className={styles.header}>
                <img src={''} alt={'logo'}/>
            </header>

    )
}

export default Header;


