import {UserType} from "../../redux/types";
//
// let updateObjectInArray = (items: UserType[], itemId: number, objPropName: string, newObjProps: any) => {
//    return  items.map(i => i[objPropName] === itemId ? {...i, ...newObjProps} : i);
// }